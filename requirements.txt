# requirements.txt for SphinxOS
# Runtime dependencies for 6D TOE simulation and quantum circuit emulator
numpy>=1.21.0  # Numerical computations and array operations
scipy>=1.7.0   # Scientific computing, including integration and sparse matrices
sympy>=1.9     # Symbolic mathematics for metric tensor calculations
matplotlib>=3.4.0  # Visualization of simulation results and quantum circuit outcomes

# SphinxSkynet Node dependencies
fastapi>=0.115.0          # Web framework for node API endpoints
uvicorn>=0.30.0           # ASGI server for FastAPI
prometheus_client>=0.20.0 # Prometheus metrics exposition
pydantic>=2.0.0           # Data validation

# Security dependencies (Production)
pyjwt>=2.8.0              # JWT token authentication
cryptography>=41.0.0      # Cryptographic primitives for JWT
redis>=5.0.0              # Redis client for rate limiting and caching
pyyaml>=6.0               # YAML configuration file parsing

# Blockchain dependencies (Production)
web3>=6.11.0              # Ethereum and EVM blockchain interaction
eth-account>=0.10.0       # Ethereum account management and signing

# Monitoring and logging (Production)
sentry-sdk>=1.39.0        # Error tracking and monitoring
python-json-logger>=2.0.0 # Structured JSON logging

# Treasury and Bridge dependencies (optional)
pyyaml>=6.0       # Configuration file parsing
web3>=6.0.0       # Ethereum/blockchain interaction (optional for bridge deployment)
py-solc-x>=1.1.1  # Solidity compiler (optional for bridge deployment)

# Optional dependencies for testing
pytest>=7.0.0     # Unit testing framework
pytest-cov>=3.0.0 # Code coverage reporting for tests
pytest-asyncio>=0.21.0    # Async test support
